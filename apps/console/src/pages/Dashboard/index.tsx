// Route surface for /o/:orgId/dashboard (metrics tiles). Implementation stays at
// src/screens/index.tsx (Dashboard) alongside its test-pinned siblings; see
// pages/Tasks/index.tsx for the full note. Page-fleet restyles by rebuilding this file.
export { Dashboard as DashboardPage } from "../../screens";
