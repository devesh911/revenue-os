// Route surface for /o/:orgId/live (the live conversations monitor). Implementation
// stays at src/screens/index.tsx (LiveMonitor) — path+source test-pinned, see
// pages/Tasks/index.tsx for the full note. Page-fleet restyles by rebuilding this file.
export { LiveMonitor as ConversationsPage } from "../../screens";
