# gitleaks config (S9.3).
# History note: apps/console/.env.local (the LOCAL demo anon key — designed-public, S7.3) was
# briefly committed at 4796388 and removed same day. A path allowlist used to live here; it was
# removed 2026-07-11 because it permanently blinded the scanner to that path — the single
# historical commit is ignored by fingerprint in .gitleaksignore instead (commit-scoped, so any
# FUTURE secret landing on that path is caught again).
[extend]
useDefault = true
